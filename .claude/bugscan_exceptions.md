# Bugscan Exceptions

Known issues, accepted risks, and false positives that should NOT be reported in future scans.
Each entry should be a concise one-liner describing what to skip and why.

## Accepted Risks
- pd/np NameError when AutoGluon not installed (deployment edge case — AutoGluon is always installed in production)
- Default admin password Admin123! with no forced rotation (warning printed to stdout, acceptable for internal tool)
- Session tokens stored plaintext in sessions.json on disk (filesystem access = game over anyway)
- Server binds 0.0.0.0 by default (configurable via HOST env var, standard for containerized deployments)
- No user deletion/deactivation capability for active users (feature gap, not a bug)
- No CSRF token on POST endpoints (mitigated by SameSite=Strict cookies)
- SameSite cookie not supported in older browsers (irrelevant for internal tool with modern browsers)
- time.time() used for rate limiting instead of time.monotonic() (NTP jumps negligible for minute/hour windows)
- SimpleRateLimiter / APIRateLimiter memory growth (bounded by total unique users, small for internal tool)
- No pagination on get_all_models (bounded by 50-model-per-user quota)
- Spin-wait in model_cache.load_model with time.sleep(0.2) (suboptimal but functional, low real-world impact)
- _session_store._clean_expired O(n) on every session create (bounded by session count, internal tool)
- _safe_read_json creates unlimited .corrupt.bak files (requires persistent corruption, extremely unlikely)
- Orphan model directory cleanup could hit in-progress training (1-hour guard is generous)
- In-memory Airflow ZIP export can use significant RAM for large models (rare admin operation)
- No max length on audio transcripts stored in model metadata (acceptable at call center scale)
- Global 200MB upload limit may block multi-file audio uploads (generous for typical call center audio)
- handle_change_password verifies password against potentially stale data (race requires same user from two sessions simultaneously)
- Airflow ZIP export size check only measures agmodel directory, not full ZIP (minor, model files dominate size)
- _embedding_cache_evictor daemon thread started at module import time (server.py never imported as library)
- _session_store.periodic_flush thread started at module import time (same — never imported as library)
- ResourceManager instantiated at module scope with GPU/RAM probing (same — never imported as library)
- Blob URL minor memory leaks in frontend download functions (cleaned up on page navigation)

## False Positives
- _cors_allowed as class variable evaluated once at import (standard pattern for env-var config, not a bug)
- escAttr double-encoding & character (Step 1 JS doesn't touch &, Step 2 HTML encodes it — correct order)
- _safe_filename allows Unicode in Content-Disposition (intentional, prevents response splitting only)
- Multipart parser strips trailing -- from file content (only triggers for malformed bodies; well-formed bodies have closing marker in separate section)
- Whisper double-checked locking race returns None (min_idle_seconds guard makes extremely unlikely, error caught per-file)
- LLM prompt injection via schema variable names (users control their own evaluation schemas, no cross-user impact)
- Wilson CI math.sqrt negative argument (p clamped to [0,1], expression always >= 0)
- feature_columns_embedded not saved to metadata (conditional save matches conditional use — both gated on text_columns)
- handle_predict semaphore release without matching acquire (return before try block, finally never executes)
- torch NameError in _ensure_vram_available second try block (caught by except Exception)
- clean_dataframe context="preview" undefined behavior (correct fallthrough to standard tabular cleaning)
- parseCSVLine doesn't handle CRLF in quotes (quote-aware line splitter handles this upstream)
- renderMd regex injection via pre-escaped content (esc() called first, all captures are safe)
- Frontend doesn't handle 503 on upload semaphore (api() receives JSON body and displays error correctly)
- handle_predict_batch double body Content-Type read (single header read per handler, not a bug)
- _per_model_locks unbounded growth (non-existent models get throwaway RLock, not cached)
- _atomic_write_json temp file naming collision (all writes serialized by _file_locks RLock)
- find_user returns mutable reference to dict (load_users re-reads from disk, fresh list each call)
- _ensure_vram_available returns stale VRAM check (defensive code, subsequent operations verify)
- Audio temp dir not cleaned up on error (cleanup IS in finally block, handles all paths)
- CORS wildcard + credentials ambiguity (code correctly omits Allow-Credentials when wildcard)
- JobStore.__getitem__ non-atomic mutations (GIL makes dict __setitem__ atomic in CPython)
- Integer overflow in _wilson_ci for large datasets (math is sound with clamped inputs)
- Unquoted HTML class attribute in renderCallAnalysisResults (valid HTML, no spaces in class name)
- Feature input element ID collision on similar column names (values collected via data-feature attribute, not getElementById)
- Bare torch reference in OOM error handler (NameError caught by except Exception: pass, no crash)
- Password change reports success if user deleted concurrently (impossible race, self-harm only)
- MSSQL export loads model without _prediction_semaphore (rare admin op, model usually cached, quick SQL generation)
- Audio predict jobs submitted to _audio_eval_queue with wrong job store (microsecond gap, pipeline sets status itself)
- Non-atomic job status+error dict updates (microsecond window, next poll correct)
- handle_predict_batch doesn't validate rows is a list (caught by outer exception handler)
- Early auth check for large uploads doesn't enforce API rate limit (upload semaphore limits to 5)
- ACF max_lag negative with few data points (len(y)>=10 ensures max_lag>=4)
- Overfit warning never shown for regression (by design, different scales)
- Cost estimation hardcoded sample_size=100 (cost estimation is inherently approximate)
- POST body read before route matching enables medium RAM use (bounded by connection limit ~50*10MB)
- sklearn tree SQL divisor uses len(tree_sqls) not len(estimators) (correct: avg of included subset)
- Trend strength positional addition after dropna() (trend and resid always have NaN at same positions in additive decomposition)
- CSV row count failure sets total_rows=0 bypassing limit (hard to trigger, upload limit provides real constraint)

- Stale job cleanup double-release of model quota for audio_eval (mitigated by _stale_released flag, microsecond window)

## Already Fixed
- Zip extraction path traversal (added member path validation)
- _load_only bypasses all resource/concurrency guards (added semaphore + ref counter)
- Batch prediction CSV encoding bypass (added encoding fallback chain)
- Training quota leak on model_ref_counter.acquire failure (added release_model_quota)
- Missing Content-Length on login response (compute body_bytes first, add header)
- Content-Length off-by-one on 503 response (changed 30 to 29)
- _llama_process.kill() without None guard (added if not None check)
- handle_approve_user / handle_reject_user missing .lower() on username
- handle_update_role TOCTOU race (re-verify user exists and role inside lock)
- IP rate limiter reset clears other users' brute-force attempts (only reset per-user key)
- No max password length allows hash DoS (capped at 128 characters)
- Dead code in train_model finally block (removed unreachable temp-dir cleanup)
- Stale job cleanup over-releases model quota for non-training jobs (added action_type guard)
- Missing submodel validation in handle_predict and handle_predict_batch (added validation)
- Audio jobs missing _resource_task_id/_model_id in job dict (added metadata fields)
- Audio pipeline per-file progress doesn't update _updated_at heartbeat (changed to update_fields)
- Upload semaphore timeout doesn't drain request body (added rfile.read before 503)
- Logout response missing Content-Length header (added header)
- handle_export_mssql loads model without model_ref_counter (added acquire/release)
- Correlation analysis NaN target: pd.factorize maps NaN to -1 not corrected (added nan fix)
- Timeseries prediction KeyError when ts_col missing from history (added column check)
- Audio predict file picker accepts any file type (added extension filter)
- Audio drop handlers inconsistent webm support (unified regex across all handlers)
- good_quality preset silently downgraded in timeseries training (added to whitelist)
- Airflow ZIP uses backslash paths on Windows (changed to .as_posix())
- Airflow export missing submodel name validation (added validation)
- Explainability reads CSV without encoding fallback (replaced with _read_csv_with_fallback)
- Stale comment from dead code removal in train_model (removed)
- Audio eval empty-transcript branch missing update_fields heartbeat refresh (changed to update_fields)
- Audio predict empty-transcript branch missing update_fields heartbeat refresh (changed to update_fields)
- Batch timeseries prediction KeyError on missing timestamp column (added KeyError to except)
- Seasonal decomposition index-misaligned Series addition (switched to .values numpy arrays)
- Audio file name collision after sanitization in eval and predict (prepended index to filenames)
- Case-sensitive self-role-change guard in handle_update_role (added .lower())
- Path traversal startswith check missing trailing os.sep in tar/zip extraction (added os.sep)
- Static file serving startswith check missing trailing os.sep (added os.sep)
- MSSQL export filename not sanitized for filesystem-unsafe chars (added regex sanitization)
- Airflow DAG text embedding code gated on TASK_TYPE=="text" which is never set (changed to TEXT_COLUMNS)
- Missing CORS credentials+expose headers on Airflow ZIP export responses (added matching send_file_download pattern)
- Missing target column validation in single timeseries prediction (added column check)
- Airflow ZIP export not protected by model_ref_counter (added acquire/release)
- Training heartbeat gap during fit() and post-processing (added update_fields at checkpoints)
- Stale-job double-release of model_ref_counter/resource_manager (added _stale_released flag)
- Llama restart loop with no cooldown after failure (added 5-minute cooldown)
- Audio eval/predict pipelines missing _stale_released guard (added same pattern as training)
- handle_explain tabular path missing _prediction_semaphore (added semaphore acquire/release)
- sklearn tree ensemble SQL missing truncation warning for >20 estimators (added warning)
- Frontend CSV line splitter doesn't handle RFC 4180 escaped quotes (added "" handling)
- Timeseries explainability NaT index crash on isoformat() (added pd.notna guard)
- Overfit warning never shown for timeseries models (enabled with score-score_internal formula)
- Blank CSV lines create ghost rows in timeseries predictions (added empty line filter)
- Seasonal decomposition seasonal+resid positional misalignment (fixed with index intersection)
- audio_predict_pipeline updated_meta["name"] KeyError on corrupt meta (changed to .get())
- CORS preflight advertises PUT/DELETE but no handlers exist (removed from allowed methods)
- Audio eval/predict pipeline temp file leak on model_ref_counter.acquire failure (added cleanup before early return)
- sklearn tree SQL val.argmax() returns 0 for regression instead of leaf value (added shape check, use val.flat[0] for regression)
- Airflow DAG clean_dataframe identifies error rows but doesn't filter them from predictions (added df = df[~error_mask])
- handle_update_role loop uses exact match against lowered target_username (added .lower() to u["username"])
- handle_change_password loop exact match (both sides have original casing from same file, always match)
- Audio predict status missing queue position (added position field and frontend check)
