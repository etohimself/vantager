# ═══════════════════════════════════════════════════════════════
#  Tahmin Platformu — Python Dependencies
#  Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════

# ── PyTorch with CUDA 12.1 support (broadest driver compatibility) ──
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.4.0,<3.0

# ── AutoGluon (ML engine) ──
autogluon.tabular[all]>=1.1,<2.0
autogluon.timeseries>=1.1,<2.0

# ── AutoGluon MultiModal (optional, for advanced text/NLP) ──
# Requires GPU for practical use. Comment out if CPU-only.
autogluon.multimodal>=1.1,<2.0

# ── Speech-to-Text (Whisper) ──
faster-whisper>=1.0.0,<2.0
ctranslate2>=4.0.0,<4.5  # 4.5+ requires CUDA 12.3+ and cuDNN 9; stick with 12.1-compatible builds

# ── Text Embeddings (NLP without heavy GPU training) ──
sentence-transformers>=3.0.0,<4.0

# ── HTTP client (for llama.cpp LLM API calls) ──
requests>=2.31.0,<3.0

# ── Data / ML utilities ──
pandas>=2.0.0,<3.0
numpy>=1.24.0,<3.0
scikit-learn>=1.3.0,<2.0
psutil>=5.9.0,<7.0

# ── Analysis extras (used in feature importance / diagnostics) ──
shap>=0.43.0,<1.0
statsmodels>=0.14.0,<1.0
